export interface Boxvis {
  margin: {
    horizontal: HTMLElement;
    vertical: HTMLElement;
    inner: HTMLElement;
  };
  border: {
    horizontal: HTMLElement;
    vertical: HTMLElement;
    inner: HTMLElement;
  };
  padding: {
    horizontal: HTMLElement;
    vertical: HTMLElement;
    inner: HTMLElement;
  };
  box: {
    horizontal: HTMLElement;
    vertical: HTMLElement;
    inner: HTMLElement;
  };
  tooltip: HTMLElement;
}
